# -*- coding: utf-8 -*-
"""提示词模板：冷启动 / 热更新提案 / 监督判断（分离，便于按 Agent 调整）。"""

# ---------------- 冷启动：归纳初始任务体系 ----------------
# 边界判据（v0.2 2026-08-21 初版 → v0.3 同日精细化）：
# 收录标准 = 「信息技术岗位的工作职责」，而非「任务本身是否技术性」。
# v0.2 的一刀切"只收 IT 任务"过严——IT 岗位（尤其技术经理/主管）事实上承载组织管理、
# 人员培养、内部培训、跨部门协同等通用职责，应收；只有传统行业/专职职能的**核心业务任务**
# （它们是非 IT 岗位的主职）才排除。三个任务 Agent 共用这一双向口径。
NON_IT_DOMAINS = ("化学/生物/材料实验检测、机械设计与制造工艺、电气/电力工程、土木/建筑、"
                  "供应链/采购/物流职能、财务/会计、法务/合同、专职行政/人事、市场/运营/销售、"
                  "质量检验/体系认证、光学/精密仪器、安全环保（EHS）")
IT_GENERIC_DUTIES = ("团队与部门组织管理、人员招聘与绩效、新人培养与内部培训、"
                     "知识分享沉淀、跨部门沟通协同、IT 服务/外包供应商的技术对接")

PROMPT_COLD_START = """你是岗位任务体系构建专家。以下是 {n} 条招聘JD的片段。请归纳这些JD中出现的工作任务类别（Task），形成一份**初步的简要任务体系**。

要求：
- 任务类别语义清晰、相互独立、**尽量精简**（约 {min_n}~{max_n} 个，不要过细）
- 每个任务给出：中文名、英文名、一句话描述
- **名称简洁自足**：中文名通常 4-12 个汉字（上限 14），优先「动作+对象」或「限定+核心」结构，避免堆砌"的/与/及"、避免重复词，脱离上下文可理解
- 面向信息技术/数字经济领域岗位
- **收录判据 = 是否为信息技术岗位的工作职责**（而非任务本身是否"技术"）：
  - IT 岗位职责中的组织管理与人员培养（{it_generic}）**属于本体系**，应正常提取为任务；
  - 传统行业/专职职能的核心业务任务（{non_it}）**一律忽略**——样本中可能混有此类非 IT 岗位 JD；
  - 信息技术相关的售前/售后技术支持、IT 项目管理照常提取

严格只输出 JSON 数组，不要任何其他文字，格式：
[{{"name_zh":"大模型应用开发与集成","name_en":"LLM Application Development & Integration","description":"利用大模型API构建业务应用，进行检索增强、智能体编排等"}}]

招聘JD片段：
{docs}"""

# ---------------- 热更新：提案 Agent ----------------
PROMPT_PROPOSE = """你是岗位任务体系维护专家。以下**当前全部已有任务**（共 {n_tasks} 个，是唯一存在的任务，新提案不得与它们重复）：
{labels}

以下是新一批招聘JD片段。请**先做覆盖映射，再决定是否更新**：

**第1步｜映射（必须，先于一切提案）**：JD 中出现的每个工作任务，先映射到现有任务 code——
- 语义充分接近即视为已覆盖（名称不必一致）；
- **更具体的子项（下位概念）也视为已覆盖**，不新增；
- 绝大多数内容都应能映射到现有任务。

**第2步｜找缺口**：只有**完全无法映射**、且属于**新的大类层级**的内容才是候选。
- 现有任务名称过窄导致覆盖不足 → 用 `modify` 扩宽它，**而不是 `add`**；
- 两个现有任务语义相近 → 用 `merge` 合并。

**第3步｜add 前自查（每一条都必须通过，否则删除该提案）**：
- ❌ 属于**传统行业/专职职能的核心业务任务**（{non_it}）→ 超出体系范围，直接忽略，禁止 add。
  注意判据看**岗位**而非任务标签：IT 岗位（技术经理/主管/资深工程师等）职责中的
  {it_generic}**不属此列**，按正常任务参与映射与提案；
- ❌ 名称与上方 labels 中任一任务**相同或近似**（含只是更具体子项）→ 这不是新增，禁止 add；
- ❌ 是**岗位名 / 工种名 / 具体技术栈** → 不是抽象任务类别；
- ❌ 本批已提过 → 重复提案。

**输出偏好（原则性，不是数量指标）**：
- **默认 covered=true**：若本批内容都能映射到现有任务（非 IT 域内容直接忽略、不影响判定），直接 covered=true、`updates` 为空数组；
- `add` 应极其保守：只有真正出现**无法并入任何现有任务的新大类**时才新增，宁缺毋滥；
- 内容可并入现有任务时，优先 `modify` 扩宽或 `merge` 合并，而非 `add`。

示例：
- JD 提到"负责芯片封装工艺开发"，体系已有 T-08 半导体工艺与制造 → covered=true（下位概念），不得 add。
- JD 提到"负责公司法务与合规审核"（法务岗）→ 传统职能域核心任务，忽略即可，**不得 add**。
- JD 提到"负责研发团队管理、人员招聘与绩效评估、新人培养"（技术经理岗）→ 属 IT 岗位的
  组织管理职责，**不算非 IT 域**：体系已有对应任务则映射，无则可 add（如"技术团队管理"）。

每个提案必须给出理由。严格只输出 JSON 对象，不要任何其他文字，格式：
{{"covered": true或false, "updates": [{{"action":"add|merge|modify", "task":{{"name_zh":"","name_en":"","description":""}}, "merge_codes":["T-01","T-02"], "target_code":"T-01", "reason":"一句话理由"}}]}}

新招聘JD片段：
{docs}

【输出前最后自查】把你准备输出的每个 add 名称，与上方已有任务逐个比对：
- 与任一已有任务**同名或近似同名** → 删除该提案（它不是新增，已存在）；
- 是已有任务的下位概念 → 删除该提案；
- 确认后，再输出 JSON。"""

# ---------------- 热更新：监督 Agent ----------------
PROMPT_SUPERVISE = """你是任务体系**监督员**。核心原则：
1. 任务体系应**尽量精简**，禁止不必要的膨胀；
2. 语义充分接近的任务应视为**同一任务**——即使名称不同、描述不完全一致，也不得为此新增任务；
3. **任务粒度契约**：任务是抽象的岗位工作类别，不是岗位名/工种名/具体技术栈。
   若提案属于某现有任务的**下位概念**（更具体的子项），必须**拒绝**并指出应并入的现有任务（map_to），
   不得批准为新增；
4. `add` 应**极端保守**：只有无法并入任何现有任务、且属于**新的大类层级**的任务才批准；
   疑似过细（岗位级/工种级/技术栈级）的提案一律拒绝；
5. **非 IT 域零容忍（按岗位判据）**：提案若属传统行业/专职职能的核心业务任务（{non_it}），
   一律**拒绝**（理由注明"非 IT 域"），无论它是否为新大类；
   但 IT 岗位职责内的{it_generic}**不属于非 IT 域**——按正常精简原则裁定，不得以"不技术"为由拒绝；
6. 当前体系任务总数已达 {total_tasks}（合理规模约 30~60），越接近上限越应只批准极必要的新类别。

请逐条判断以下提案：
- `add`：被现有任务覆盖 / 是其下位概念 / 过细（岗位级）/ 非 IT 域 → **拒绝**并给出 map_to（非 IT 域无需 map_to）；确属全新大类才批准
- `merge`：两个任务语义确实相近才批准，否则拒绝
- `modify`：修改确实更准确才批准，否则拒绝

严格只输出 JSON 对象，格式：
{{"decisions": [{{"index":0, "approved":true或false, "reason":"一句话", "map_to":"（add被拒时建议合并到的现有code，可为null）"}}]}}

当前任务体系（code:name）：
{labels}

更新提案：
{proposals}"""

# ================= 技能体系（Skill 层）提示词 =================
# Skill = 稳定的能力类别（跨越数年）；具体工具/框架/语言名属 SkillPoint 层，不在此层。
# 输出格式与文献版 skills0805.json 的 detail 兼容（code/name_zh/name_en/definition/skill_type）。

# ---------------- 技能：冷启动 ----------------
PROMPT_SKILL_COLD_START = """你是岗位技能体系构建专家。以下是 {n} 条招聘JD的片段。请归纳这些JD中涉及的**技能类别（Skill）**，形成一份**初步的简要技能体系**。

要求：
- **技能是稳定的能力类别**（如"机器学习与深度学习""数据分析""项目管理"），
  **不是**具体工具/框架/语言名（如"LangChain""Docker""Java"属于技能点 SkillPoint，不在此层）
- 每条技能给出：中文名、英文名、定义（能力描述）、skill_type
  - skill_type：`hard`=技术技能 / `soft`=基础通用技能 / `hybrid`=兼具
- 面向信息技术/数字经济领域岗位（AI、大数据、操作系统、算法等）
- **收录判据 = 是否为信息技术岗位的工作职责**：IT 岗位职责中的管理与协作素养
  （{it_generic}）照常提取；传统职能核心业务任务（{non_it}，样本中混有此类 JD）一律忽略
- 技能类别语义清晰、相互独立、尽量精简（约 {min_n}~{max_n} 个，不要过细）
- **名称简洁自足**：中文名通常 4-12 个汉字（上限 14），优先「动作+对象」或「限定+核心」结构，避免堆砌"的/与/及"、避免重复词，脱离上下文可理解

严格只输出 JSON 数组，不要任何其他文字，格式：
[{{"name_zh":"机器学习与深度学习","name_en":"Machine Learning & Deep Learning","definition":"掌握机器学习模型构建、训练与评估的能力","skill_type":"hard"}}]

招聘JD片段：
{docs}"""

# ---------------- 技能：热更新提案 Agent ----------------
PROMPT_SKILL_PROPOSE = """你是岗位技能体系维护专家。以下**当前全部已有技能**（共 {n_tasks} 个，是唯一存在的技能，新提案不得与它们重复）：
{labels}

以下是新一批招聘JD片段。请**先做覆盖映射，再决定是否更新**：

**第1步｜映射（必须，先于一切提案）**：JD 中出现的每个技能，先映射到现有技能 code——
- 语义充分接近即视为已覆盖（名称不必一致）；
- **更具体的子项（下位概念）视为已覆盖**，不新增；
- **具体工具/框架/语言名（LangChain、Docker、Java 等）属于技能点 SkillPoint，不是新技能**；
- 绝大多数内容都应能映射到现有技能。

**第2步｜找缺口**：只有**完全无法映射**、且属于**新的大类能力**的内容才是候选。
- 现有技能定义过窄导致覆盖不足 → 用 `modify` 扩宽，**而不是 `add`**；
- 两个现有技能语义相近 → 用 `merge` 合并。

**第3步｜add 前自查（每一条都必须通过，否则删除该提案）**：
- ❌ 名称与上方 labels 中任一技能**相同或近似**（含只是更具体子项）→ 已存在；
- ❌ 属于**传统职能核心业务**（{non_it}）→ 超出体系范围，直接忽略，禁止 add；
  IT 岗位职责中的{it_generic}不属此列，按正常技能处理；
- ❌ 是**具体工具/框架/语言名 / 岗位名** → 不是稳定能力类别；
- ❌ 本批已提过 → 重复提案。

**输出偏好（原则性，不是数量指标）**：
- **默认 covered=true**：若本批内容都能映射到现有技能，直接 covered=true、`updates` 为空数组；
- `add` 应极其保守：只有真正出现**无法并入任何现有技能的新大类能力**时才新增，宁缺毋滥；
- 内容可并入现有技能时，优先 `modify` 扩宽或 `merge` 合并，而非 `add`。

示例：
- JD 提到"熟悉 LangChain、RAG 应用开发"，体系已有"大模型应用开发" → covered=true（工具名属 SkillPoint），不得 add。
- JD 提到"负责芯片 DFT 设计验证"，体系无任何芯片设计类技能 → 可 add 一条"芯片设计与验证"。

每个提案必须给出理由。严格只输出 JSON 对象，不要任何其他文字，格式：
{{"covered": true或false, "updates": [{{"action":"add|merge|modify", "task":{{"name_zh":"","name_en":"","definition":"","skill_type":"hard|soft|hybrid"}}, "merge_codes":["S-01","S-02"], "target_code":"S-01", "reason":"一句话理由"}}]}}

新招聘JD片段：
{docs}

【输出前最后自查】把你准备输出的每个 add 名称，与上方已有技能逐个比对：
- 与任一已有技能**同名或近似同名** → 删除该提案（已存在）；
- 是已有技能的下位概念 → 删除该提案；
- 确认后，再输出 JSON。"""

# ---------------- 技能：热更新监督 Agent ----------------
PROMPT_SKILL_SUPERVISE = """你是技能体系**监督员**。核心原则：
1. 技能体系应**尽量精简**，禁止不必要的膨胀；
2. 语义充分接近的技能应视为**同一技能**——即使名称不同、定义不完全一致，也不得为此新增；
3. **技能粒度契约**：技能是**稳定的能力类别**，不是具体工具/框架/语言名（属 SkillPoint 层），
   也不是岗位名。若提案属于某现有技能的下位概念，必须**拒绝**并指出应并入的现有技能（map_to）；
4. `add` 应**极端保守**：只有无法并入任何现有技能、且属于**新的大类能力**的技能才批准；
   疑似过细（工具级/岗位级）的提案一律拒绝；
5. **非 IT 域零容忍（按岗位判据）**：传统职能核心业务（{non_it}）一律**拒绝**（理由注明"非 IT 域"）；
   IT 岗位职责内的{it_generic}不属非 IT 域，按正常精简原则裁定；
6. 当前体系技能总数已达 {total_tasks}（合理规模约 30~60），越接近上限越应只批准极必要的新增。

请逐条判断以下提案：
- `add`：被现有技能覆盖 / 是其下位概念 / 过细（工具级、岗位级）/ 非 IT 域 → **拒绝**并给出 map_to（非 IT 域无需 map_to）；确属新的大类能力才批准
- `merge`：两个技能语义确实相近才批准，否则拒绝
- `modify`：修改确实更准确才批准，否则拒绝

严格只输出 JSON 对象，格式：
{{"decisions": [{{"index":0, "approved":true或false, "reason":"一句话", "map_to":"（add被拒时建议合并到的现有code，可为null）"}}]}}

当前技能体系（code:name）：
{labels}

更新提案：
{proposals}"""
