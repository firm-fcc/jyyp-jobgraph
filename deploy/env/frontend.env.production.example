# 前端构建期环境变量样例，复制为 frontend/.env.production 后填入实际域名。
#
# Vite 在 `npm run build` 时读取 .env.production，取值被写死进构建产物，
# 因而域名变更后必须重新构建，改服务器配置不生效。
#
# 前后端同域发布，此处填站点自身的地址即可，浏览器发出的是同源请求。
# 留空或不设此项时，人岗匹配页整体回落演示链路，其余各页不受影响。

VITE_MATCH_API=https://example.com
